import { observer } from "mobx-react-lite";

const Loading = observer(() => <div className="min-h-screen min-w-screen flex justify-center items-center font-bold">Loading...</div>);

export default Loading;
